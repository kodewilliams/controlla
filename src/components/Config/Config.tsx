import "./Config.css";
import { FC } from "react";
import { observer } from "mobx-react-lite";

const Config: FC = observer(() => {
  return <div className='config centered'></div>;
});

export default Config;
