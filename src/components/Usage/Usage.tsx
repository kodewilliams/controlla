import "./Usage.css";
import { FC } from "react";
import { observer } from "mobx-react-lite";

const Usage: FC = observer(() => {
  return <div className='usage centered'></div>;
});

export default Usage;
